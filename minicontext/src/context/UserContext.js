import React, { createContext } from "react";


//  created the context for global state management
const UserContext = React.createContext();

export default UserContext;// here we create the store for the data
